# CSC401-Assignment3

src/LCS.java - code containing:

LCS class: computes longest common subsequence using bottom-up method
findLCS() — computes LCS length and prints table updates
printTable() — prints current dynamic programming table
backtrackLCS() — reconstructs LCS string
main() — reads sequences, runs program, prints LCS and length

sample-output.txt: sample output of the program
